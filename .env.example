# Configuração de Ambiente para Produção (VPS Hostinger)

DEBUG=False
SECRET_KEY=sua_chave_segura_aqui_para_producao
ALLOWED_HOSTS=institutoeconomiaaonatural.cocrias.com,localhost,127.0.0.1

# Configuração do PostgreSQL (mesmos dados do serviço "db" no docker-compose.yml)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=economia_natural
DB_USER=postgres
DB_PASSWORD=postgres123
DB_HOST=db
DB_PORT=5432
